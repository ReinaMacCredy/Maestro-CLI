# Handoff: Architecture Refactor -- Remaining Units

## Context

Architecture audit completed. 4/7 units merged to main and pushed to origin. 2 units lost during `git filter-branch` (node_modules purge) and need redo. These are independent of each other -- can run in parallel.

## Unit 3: Worker Namespace Consolidation

**Goal**: Move 5 worker-related files from flat `src/utils/` into `src/utils/worker/` subdirectory.

**File moves**:
- `src/utils/worker-launch.ts` -> `src/utils/worker/launch.ts`
- `src/utils/worker-prompt.ts` -> `src/utils/worker/prompt.ts`
- `src/utils/prompt-budgeting.ts` -> `src/utils/worker/budgeting.ts`
- `src/utils/prompt-file.ts` -> `src/utils/worker/file.ts`
- `src/utils/spec-builder.ts` -> `src/utils/worker/spec.ts`

**Then update all imports**:
- Internal imports between these 5 files (they reference each other)
- External consumers: `src/usecases/start-task.ts`, `src/usecases/commit-task.ts`, `src/commands/worktree-start.ts`, `src/commands/worktree-create.ts`, any test files

**Verify**: `bun run build && bun test && bun src/cli.ts --help`

## Unit 6: Command Registry Codegen

**Goal**: Auto-generate the 68 command imports in `src/cli.ts` at build time, like `src/skills/generate.ts` does for skills.

**Create**:
- `src/commands/generate.ts` -- scans `src/commands/` for `.ts` files, excludes `_`-prefixed files, `generate.ts`, `registry.generated.ts`. Generates imports + subCommands map.
- `src/commands/registry.generated.ts` -- generated output with header comment `// Auto-generated by commands/generate.ts -- do not edit`

**Modify**:
- `src/cli.ts` -- remove all 68 manual imports and hand-written subCommands map. Replace with: `import { subCommands } from './commands/registry.generated.ts'`
- `build.ts` -- add `await $\`bun src/commands/generate.ts\`` before the skills generation step

**Reference**: Follow the pattern in `src/skills/generate.ts` for the generator structure.

**Verify**: `bun run build && bun test && bun src/cli.ts --help` (compare command list is identical -- all 68 commands present)

## Conventions

- TypeScript, ES2022 target, ESNext modules
- Use `.ts` extensions in imports
- Bun runtime (`bun test`, `bun run build`)
- Commit after each unit passes build+test
- Push to `origin main` after commit

## Current state of main

- 156 tests, 0 failures
- Build: 129 modules, standalone binary
- strict: true enabled
- Hexagonal ports layer complete
- paths.ts already split into paths + fs-io + locking
